/**
 * Repository Pattern — data access layer.
 * Each repository wraps Prisma queries for a single domain entity.
 *
 * Example: booking.repository.ts → findMany, findById, create, update, delete
 */
export {};
